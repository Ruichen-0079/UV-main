# v0.1.2 Portable artifact repair

This local repair starts at c47d67027e65c99c52efa6ed5f5b56287ba14a25,
not current main or PR #315. No PR #315 implementation was inspected or reused.
The original is `/home/ruichen/桌面/yuvi-v0.1.2-linux-x64-portable`.
All 1,902 release-SBOM file hashes match. Desktop SHA256 is
`238b6c6419595be3b8830d28cceb89bfd49cc24654e6849f09e381af49025d7f`,
matching its build-provenance receipt. Both receipts identify the same commit.
The launcher and portable-state module byte-match that commit's source.
No v0.1.2 tag was returned by the remote tag query; the verified baseline is
the artifact's explicit source commit, not an assumed tag.

## Reproduction and cause

An unchanged copy was launched with its public `./yuvi` entry, first with the
normal versioned state root, then a fresh explicit state root. Installed YUVI
was already running and was not stopped or changed. A third run used a separate
network namespace to remove Installed listeners from the reproduction without
interrupting them. Timings below are fresh-state observations, relative to spawn:

| Time | Observation |
| --- | --- |
| 0 ms | Public launcher starts bundled Node/launcher. |
| 94 ms | Supervisor PID 371990 publishes authenticated control and waits for Product bootstrap. |
| 101 ms | Runtime projection is stopped/none at 16121; Mem0 already defaults to 6131. |
| 461 ms | Desktop PID 372023 is running. |
| 482 ms | ConfigService push changes Runtime to 6121, STT to 9876, TTS upstream to 9880. |
| 484 ms | Desktop reports PostgreSQL unavailable/none and abandons staged boot. |
| 5096 ms | Background refresh projects Installed Runtime healthy/external, Mem0 degraded/external, STT healthy/external. |
| 5106 ms | Launcher detects external Runtime, calls stop(), sends SIGTERM to Supervisor/Desktop. |
| 5137 ms | Launcher exits, incorrectly with status zero. |

The source path is `ConfigService::open` -> generic `UserSettings::default`
-> `supervisor_config_push` -> `POST /v1/config` -> `deriveConfigFromEnv`.
The launcher environment does not constrain that later push. The five-second
timer is `DesktopSupervisor.startBackgroundRefresh`, not a Desktop fatal timer.
The actual shutdown call is the external-ownership catch in `linux-launcher.mjs`.

Independently, packaged entry skips `bootstrap()` pending the Desktop secret.
Desktop then calls `/v1/services/postgres/start` -> `ensureService` -> generic
`startManagedIfNeeded`. The private cluster has no start command until
`prepareAndStartPrivatePostgres` runs, but that was reachable only from bootstrap.
Thus PostgreSQL never starts. Without Installed network listeners, the original
lasts 120,328 ms before the launcher readiness deadline ends it. No Portable
PostgreSQL, Mem0, Runtime, STT or TTS child was started in these original runs.

Unblocking this path exposed missing `pgcrypto` and `pg_trgm` in the released
PostgreSQL payload, both required by migration 001. Real PG16 migration tests
reproduced those failures. The package validator previously checked only vector.
The UI also checked binding once, before ownership existed; it now polls the
same Desktop binding projection with a bounded loading view.

## Ownership and configuration map

| Item | Released behavior | Repair |
| --- | --- | --- |
| Runtime | Launcher 16121, Desktop settings overwrite to 6121 | ConfigService Portable profile 16121; Supervisor rejects retargeting |
| Mem0 | Supervisor/Desktop default 6131 | 16131; owned process required |
| STT | Launcher 19876, Desktop default 9876 | 19876; stale Product local-STT URLs fail closed |
| TTS | Wrapper 19881 but settings upstream 9880 | Reserved 19881/19880; no bundled local TTS, enabling unowned local TTS fails closed |
| PostgreSQL | Private config but start command never prepared | Existing private lifecycle invoked by explicit start; preferred 55432, existing bounded allocator retained |
| Supervisor | Dynamic loopback control, version-root active pointer | Same; launcher pins pointer PID to its spawned Supervisor |
| Web fallback | Starts after owned healthy Runtime, port 15173 | Starts after control handshake; forwarding consumes pinned Supervisor owned/healthy Runtime projection |
| Desktop settings | `config/com.yuvi.companion/settings.json` | Same ConfigService; restore preferences and rebase local transport |
| Product providers | `config/product-settings.json` | Same Runtime Product authority; refuse foreign persisted STT/local TTS |
| Env files | `.env.local` overrides even launcher transport/roots | Portable fails closed if legacy env files exist; use Product settings |
| Shell | Whitelisted GUI env, language/backend; synthetic HOME/XDG roots | Same, no credentials inherited |
| Secret Service | `YUVI-portable-0.1.2` on session D-Bus | Same release-specific namespace |
| Ollama | Explicit external provider endpoint 11434 | Same; it is not adopted as a YUVI-owned service |

Default state remains `${XDG_DATA_HOME:-$HOME/.local/share}/YUVI/portable/0.1.2`,
or the existing explicit `YUVI_PORTABLE_STATE_ROOT`. It intentionally survives
package deletion and is shared by copies of the same product version, not by
Installed or different Portable versions. Subroots: config, data, cache, tmp,
supervisor, home. Private PG is `data/Postgres`; Mem0 is explicitly
`data/Mem0/{data,logs}` (original default was sibling `Mem0/{data,logs}`);
Runtime storage is `data/runtime-data`; STT storage is `data/local-stt`.
Supervisor per-instance logs and metadata are under `supervisor/instances`.

Authority remains: launcher owns the Supervisor/Desktop/Web process envelope;
Desktop attaches and supplies SecretStore/Product configuration; Supervisor owns
private PG and sidecar starts, readiness, ownership and shutdown. Mem0's packaged
wrapper invokes Runtime's existing migration-only entry before Mem0. Desktop
sequences PG -> Mem0 -> Runtime through the existing control API. Web rendering
waits on Desktop's binding projection. No second Supervisor, router or service
lifecycle state machine is added.

## Building locally

Use `build-linux-desktop.mjs`, `prepare-linux-daily.mjs`, and
`package-linux-release.mjs` from a clean repair commit. Internal version stays
0.1.2; put outputs in a clearly named local repair-test directory, never publish.
The Debian 12 PostgreSQL 16 distribution supplied through
`YUVI_LINUX_POSTGRES_HOME` must include contrib `pgcrypto` and `pg_trgm` built
from the same PostgreSQL source/configuration, plus pgvector. Existing migrations
are unchanged. The artifact's PostgreSQL source uses `--with-openssl`.

Verification includes Rust library tests, pnpm check, web build, repository
tests with real PG16 enabled, Portable package tests, and a bounded artifact
smoke alongside Installed YUVI. Final timings/artifact paths are in the local
handoff report, not a public release. No merge, push or tag change is intended.

## Recovery and follow-up audit

Recovery found a clean worktree at `549d50df464a6c11e136af70ef829b2b1b5201a5`.
The prior session had already amended `c4bd5b1` into that commit; both objects
remain available. Its only additional changes were the SQL helper result-array
handling and real-PG fixture adjustments. No recovery reset, clean, stash or
history rewrite was performed. The emitted memory SQL `.js`/`.d.ts` files remain
in the pre-existing `ts-emitted-backup` directory outside the source tree.

The multi-statement fix is required: node-postgres returns a result array for
batched SQL, and the prior log shows `rows.map` failing after migration 001.
The helper now flattens result rows without adding a database abstraction.
The fixture inserts the conversation session before its message, avoiding
same-statement snapshot visibility in the conversation trigger. All real SQL
migrations run unchanged against the repaired distribution and survive restart.

The follow-up removes the launcher's Runtime readiness polling/deadline entirely.
Only the authenticated control-plane handshake remains before presentation starts.
Stopped/starting/unowned service projections do not terminate the process envelope.
Preflight rejects occupied fixed ports; Supervisor refuses foreign managed services;
Desktop verifies its Runtime binding. The static fallback also gates HTTP and WS
forwarding on the existing Supervisor snapshot, pinned to the launcher's child PID,
with owned/healthy status and the expected Runtime URL. It does not probe services
or manage their lifecycle. Process exits and user shutdown still close the envelope.

`tts.enabled` is a capability preference, not proof of local ownership, so it is
no longer globally rejected. Fixed local wrapper/upstream addresses remain rebased;
Supervisor can start only configured managed commands and rejects foreign ownership.
This payload contains no local TTS binary. Product gpt-sovits/dots-tts localhost
endpoints remain rejected; explicit remote Product TTS providers remain supported.

Follow-up verification: Rust 101 passed; repository suites passed with the explicit
repaired PG16 distribution (Supervisor 223, including 5 real PostgreSQL tests);
packaging 77 passed; pnpm check and diff whitespace validation passed. Repository
integration skips require a separately supplied DATABASE_URL; the real private-PG
lifecycle/migration suite did run. Final build and bounded smoke evidence is retained
outside the checkout in the local handoff report.

The real packaging run additionally exposed a baseline Python wrapper defect:
`linux-baseline-python.mjs` forced the repository root as cwd while Mem0/STT
PyInstaller specs resolve their service source from cwd. It now preserves the
caller cwd within the mounted checkout and rejects callers outside that checkout.
The build environment remains cleared. This is a packaging path correction,
not a service lifecycle change.

A second packaging defect was the Linux Mem0 validator's Windows-derived
minimum of 1,001 files. The untouched released Linux payload has 871 files;
the real rebuilt payload has 873 and is 142 MB. Pure Python lives in the
PyInstaller archive. Validation now checks concrete Python runtime, standard
library archive, CA bundle and pinned Mem0 metadata instead of the platform file
count, retaining size, wrapper, manifest and leakage checks. A regression test
accepts the smaller valid layout and rejects missing libpython. Real bootstrap
smoke additionally exercises the frozen application's imports.

STT native-input attribution also omitted Debian `libuuid1`, although the
untouched artifact ships `libuuid.so.1` and its inventory attributes that exact
library to libuuid1 2.38.1-5+deb12u3. The source allowlist now includes this
package; existing byte matching, package version/source, and copyright copying
remain mandatory. No native-input validation was bypassed.

The public artifact audit caught two grpc build-only inputs collected by its
PyInstaller hook: a C header and Python type stub. Linux Mem0 packaging now omits
SDK headers/type stubs from `_internal`; runtime binaries/data are retained and
the unchanged public audit remains authoritative.

The first artifact smoke reached owned PostgreSQL and Mem0, applying all 12
unchanged migrations, and remained alive past five seconds. It exposed another
released bootstrap bug: the Supervisor HTTP route accepted only `[a-z_]`, so
Desktop's `/v1/services/mem0/start` returned 404 despite Mem0 being a registered
service. The route now permits digits and still validates the service ID against
the existing allowlist. An HTTP regression test covers mem0 start/restart/stop
and rejection of unknown mem1. Lifecycle ownership stays in the same Supervisor.

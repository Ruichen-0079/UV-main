# Production reachability closure

Audit baseline: GitHub `main` at `a0a277068e466a2908f0c0b2191e3ecf921e257c` (fresh fetch). This audit covers current claims in the roadmap, Campaigns A–I, and the subsequent Linux release gate. Historical atom plans do not extend the production scope. `LIVE` means there is a real product caller and observable result; configured provider credentials, local weights, and device availability remain prerequisites. Tests alone are not the reachability evidence.

## Defects corrected

- P8 main reconstruction and its Character ABI projection were exported but had no production caller. Character independently assigned `KNOWN` to identity, style, and relationship PromptBuilder text. Runtime now reconstructs P8 on each turn and supplies its typed sections to both initial Character and Cognition re-entry. The existing authored production persona has moved to P8; no second profile system or learned relationship inference was added.
- P8 correction persistence had only a PostgreSQL library adapter. The local production adapter uses the existing append/load and lineage contract, under `YUVI_RUNTIME_ENV_DIR/p8-corrections.json`. The protected controller route validates P8 authority before appending. Corruption is `ERROR`, never an empty correction history.
- Acoustic enrollment did not bind a person, and committed speech did not resolve a person before Memory use. The Local intelligence panel now offers an explicit person binding. Runtime writes trusted binding evidence through Memory and persists only its event references in `voice-binding-references.json`. On every speech turn it loads those exact Memory events and calls P8 resolution. Missing, failed, conflicting, or mixed-speaker evidence cannot select a person or enable durable person-scoped Memory. `/v1/voice/message` now commits the server-owned STT observation instead of accepting caller identity assertions as recognition.
- Production selected simple Cognition despite the existing bounded capability-aware implementation. The normal callback now uses the current read-text round-trip, Runtime capability admission, observation normalization, continuation, and Character re-entry. One explicitly authorized file, one round, no loop, no generic orchestrator. The concrete path never comes from Cognition text.
- Memory vNext's existing `characterProjection` was not consumed. Its projection also omitted ordinary long-term hits when no associative/episode hit existed. Memory now projects admitted long-term evidence, provenance, partial/limited outcomes, and thin temporal uncertainty; Runtime passes that output to `projectMemoryVNextToCharacterAbi`. It no longer reconstructs facts from PromptBuilder text.
- Proactive decision and continuation used the legacy profile prompt. They now consume the same producer-owned projections. Their separate NO_OP / REQUEST_TEXT protocol, Runtime admission, and output-language preference remain intact.

## Reachability matrix

Every server row passes through `server.ts → createAppContext` in `context.ts`, which constructs the current Runtime and ProviderRegistry. Reload constructs the same bindings against the new registry. Domain-specific controller/read-only administration rows intentionally stop at their service instead of manufacturing a conversational Runtime turn.

| Capability                                                                    | Production entrypoint                                                            | Real production caller → semantic seam → observable result                                                                                                                                                                             | Result       |
| ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| P8 identity/persona/relationship reconstruction                               | Product chat, `/message`, `/v1/messages/stream`, committed voice                 | `prepareChatPrompt → attachSemanticContext → reconstructP8MainProfile → projectP8ReconstructionToCharacterAbi → character-runtime → Chat response`                                                                                     | LIVE         |
| P8 explicit relationship corrections/restart                                  | Local controller `POST /p8/corrections`                                          | `registerLocalServiceRoutes → runtime.appendP8Correction → P8 validation → FileP8CorrectionStore → next-turn reconstruction`, including restart                                                                                        | LIVE         |
| Voice-to-person binding                                                       | Local intelligence → “Bind voice to person”                                      | `apiClient.bindVoiceProfilePerson → /voice-profiles/:id/person → Runtime.bindVoiceProfileToPerson → admitVoiceProfilePersonBinding → Memory.writeEvent`                                                                                | LIVE         |
| Voice person scope                                                            | Voice mode/PTT → `/v1/audio/transcriptions` → speech commit; `/v1/voice/message` | `commitSpeechTurn → scopeVoiceTurn → exact Memory evidence → resolveP8VoicePerson → person-scoped Memory/P8 → semantic Character speaker view`                                                                                         | LIVE         |
| Memory vNext / Character ABI                                                  | Ordinary text/voice turns                                                        | `retrieveMemories → MemoryProvider → MemoryContextBuilder → assembleMemoryVNextContext.characterProjection → projectMemoryVNextToCharacterAbi → Character response`                                                                    | LIVE         |
| L0/L1, thin temporal, associative recall                                      | Conversation and next turn                                                       | `restoreDirectContext / assembleHierarchicalMemory → conversation repository / RecentEpisodeStore → Memory vNext projection`; restart reconstruction remains available                                                                 | LIVE         |
| Bounded Cognition                                                             | Character `NEED_COGNITION` during a normal turn                                  | `Runtime.executeCharacterTurn → executeProductionCognition → executeServerCurrentReadTextCognitionRoundTrip → normalized result → generateAfterCognition`                                                                              | LIVE         |
| Read-only capability                                                          | Local controller `POST /capabilities/read-text/authorize`, then ordinary message | Runtime takes the session's one-use authorization; `REQUEST_CAPABILITY → admitRuntimeCapabilityRound → read_text_file → createServerMcpReadTextObservation → executeServerObservationContinuation → Character`                         | LIVE         |
| Proactive                                                                     | Consent UI, `/v1/proactive/consent`, live stream / explicit proactive stream     | `Runtime.armProactiveWake / streamAssistantInitiatedTurn → admission/revision gate → decision provider → NO_OP or continuation → committed assistant event / SSE`                                                                      | LIVE         |
| STT / interruption                                                            | Voice mode/PTT / live speech                                                     | `LocalSTTProvider` or selected STT chain → typed observation → Runtime capture fence; speech-activity route updates Runtime activity and cancellation                                                                                  | LIVE         |
| TTS / synchronized subtitles                                                  | Assistant reply → speech segment / Companion queue; `/v1/tts`                    | `SpeechSegmenter / SpeechQueue → selected TTSProvider → audio playback → mouth envelope and speech-synchronized subtitle bus`                                                                                                          | LIVE         |
| Provider fallback                                                             | Normal Chat, Cognition, STT, TTS and embedding calls                             | `ProviderRegistry.get*Provider → Fallback*Provider → provider error/replay policy → selected provider`; attempted/final provider metadata reports the result. On-demand vision deliberately disables fallback                          | LIVE         |
| Visual Grounding                                                              | Explicit user turn whose Character requests visual evidence                      | `requestVisualEvidence → Runtime capture/revision/cancellation gate → KDE capture → VisionProvider → bounded ephemeral observation → same-turn Character`; unavailable is observable                                                   | LIVE         |
| Embodied Presentation                                                         | Character response presentation intent                                           | `context.embodiedPresentation.propose → Runtime record initialization/admission → executeRuntimeEmbodiedPresentation → EmbodiedPresentationBridge → Companion behavior controller → outcome route → Runtime record advancement/events` | LIVE         |
| Output Language                                                               | Existing settings control / `/settings/runtime`                                  | `parseRuntimeConfig → Runtime.outputLanguage → Character ABI initial/re-entry and proactive instruction → response language metadata → speech/subtitle projection`; no STT-language inference persists a preference                    | LIVE         |
| Conversation/settings restart                                                 | Normal messages and existing settings save                                       | `ConversationRepository / finalized ingestion ledger / settings env files → createAppContext and restoreDirectContext/recovery → UI history and next-turn context`                                                                     | LIVE         |
| Acoustic profile persistence                                                  | Local intelligence enrollment/list/delete                                        | `LocalSTTProvider.voiceProfiles → local-stt/server.py → SpeakerStore → speakers.json / speakers.npz → next recognition`; embeddings remain entirely inside the acoustic adapter                                                        | LIVE         |
| Memory administration/maintenance                                             | Memory console / explicit maintenance action                                     | `routes/memory → MemoryService / MemoryMaintenanceService / scheduler.run → scoped result/status`; automatic startup/periodic sweeps remain opt-in and off by default                                                                  | LIVE         |
| Event-driven Dream consolidation                                              | Completed conversation / episode persistence                                     | `persistRecentEpisodes → DreamConsolidationEngine.consider → runDue → admitted Memory provider writes / job state`                                                                                                                     | LIVE         |
| Linux packaged start/restart                                                  | Packaged installer/user units                                                    | `prepare-linux-daily → bundled Node/supervisor → packaged Runtime entry → createAppContext`; persistent Runtime env directory is passed by supervisor                                                                                  | LIVE         |
| Generic identity mention resolution                                           | Explicit reusable P8 API only                                                    | `buildP8CurrentIdentityProjection / resolveP8IdentityMention` are bounded library APIs; the live voice path uses the separate trusted voice-person resolver, never transcript name inference                                           | LIBRARY_ONLY |
| PostgreSQL P8 correction adapter                                              | Library constructor only                                                         | `PostgresP8CorrectionStore`; the production local path selects the file adapter of the same P8 contract                                                                                                                                | LIBRARY_ONLY |
| Remote MCP transport / alternate phase compositions                           | Explicit library clients only                                                    | `createServerMcpClient`, simple Cognition and earlier embodied dispatcher/lifecycle wrappers; production uses the bounded local read-text adapter and Runtime-owned embodied execution                                                 | LIBRARY_ONLY |
| Training / broad agent loops / generic tool orchestration / continuous vision | No current product entrypoint                                                    | Existing roadmap exclusions remain unchanged                                                                                                                                                                                           | DEFERRED     |
| Idle Dream scheduling / full Temporal and Continuity systems                  | No current product entrypoint                                                    | Event-driven Dream and thin time/L0/L1 are the live bounded implementations                                                                                                                                                            | DEFERRED     |
| Windows/macOS release expansion / native Linux installer                      | No current release gate                                                          | Existing platform deferrals; current Linux packaged path remains supported                                                                                                                                                             | DEFERRED     |

`LIBRARY_ONLY` means **INTENTIONALLY_LIBRARY_ONLY**, not evidence of integration. The [export classification inventory](production-reachability-exports.json) records suspicious exported functions/classes and their reasons, including superseded wrappers and compatibility utilities. A same-file helper can be live through its owning implementation; a barrel export alone is not a caller. Historical subtitle `projectCommittedAssistantText` is now a library projection: current subtitles use the already-sanitized speech segment selected for actual playback.

## Controller contracts

The three controller routes require localhost and the configured dashboard development token. The voice UI uses the existing token transport. Acoustic profile existence is checked through the selected provider before a binding write. Binding references contain no person semantics: missing referenced Memory records fail closed. Conflicting assignments remain conflicting; automatic reassignment, acoustic confidence promotion, and transcript self-identification do not override P8.

`POST /p8/corrections` accepts the existing `p8-1e.v1` correction record. Use the current `characterInstanceId` (`yuvi-default-character-instance`), configured persona ID, explicit person `subjectScopeId`, and its exact `buildMemoryScope(personId, personaId)` reference. The live relationship interpretation target is `relationship.current`. Authored identity/persona invariants retain their existing fixed revision policy. Arbitrary conversation text does not become a persisted correction or a known relationship. No evidence is `EMPTY`, insufficient meaning is `UNKNOWN`, and unavailable/error inputs retain their states.

`POST /capabilities/read-text/authorize` accepts `{ "sessionId": "...", "path": "/absolute/file.txt" }`. The next Character turn in that session consumes the authorization even when it needs no capability. Cognition sees only an opaque read-only capability description. Runtime supplies the concrete path. The adapter accepts one regular UTF-8 text file of at most 64,000 bytes. The authorization is not persisted across restart. No authorization means an empty capability inventory; it does not select a legacy/simple bypass.

## Validation evidence

`apps/server/src/character-production-path.test.ts` boots `buildServer → createAppContext` with production Character enabled, stubbing external HTTP providers only. It exercises ordinary and streaming replies, bounded Cognition, controller P8 correction persistence across restart, the actual admitted file read and continuation, and controller voice binding across restart with resolved/mixed/conflicting scope assertions. It also verifies admitted long-term evidence and provenance in Character's actual provider request. This is the production-entrypoint smoke for the changed paths.

Focused path tests additionally cover P8 append/lineage/corruption/idempotency, all existing P8 epistemic projections, Memory vNext long-term states, Runtime Character outcomes, proactive admission/history, STT handoff/disconnects, and Visual Grounding. Required type/build and hosted exact-head Check plus Linux persistence remain merge gates. No unrelated exhaustive local regression run is part of this audit.

Hardware/model quality is not inferred from HTTP stubs. Existing provider/device paths are proven by their production callers and bounded outcomes, including honest unavailable outcomes; private sidecar deployment and current credentials remain operational prerequisites.

## Adversarial continuation pass

The previous matrix was challenged against alternate transports, automatic wakeups, reload/restart, read-only preparation, and scoped episode persistence. Counterexamples found and corrected:

- Proactive `NO_OP` preparation wrote L1 episodes. All prompt assembly now reconstructs without persisting; only admitted completed-turn persistence writes episodes.
- Non-streaming unresolved voice finalization reused the requested write flag after prompt preparation had disabled it. Turn-level memory admission now applies the same resolved speaker restriction before persistence and finalization.
- Ordinary and streaming `writeMemory=false` turns could still write L1 and trigger Dream. Completion gates now honor the write flag, and persisted user and assistant metadata excludes those turns (including failed or silent turns) from later durable episode reconstruction after restart. Read-only L0/L1 narrative reconstruction remains available.
- A shared session's whole history could be relabeled with its latest person's scope. Memory vNext filters source messages and stored episodes against the explicitly selected identity. Runtime supplies the canonical Memory scope to durable episodes and Dream.
- Live proactive SSE subscribed to the replaced Runtime after settings reload. The route now subscribes through the stable AppContext forwarding subscription.
- Automatic proactive wakeups omitted the configured person/persona. Startup and reload now pass that identity into scheduler admission and P8/Memory preparation.

Fresh pass coverage after these fixes:

| Current LIVE rows                                       | Counterexample searches and production evidence                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P8 reconstruction; explicit corrections                 | `/message`, `/v1/messages`, stream and WebSocket reach `prepareChatPrompt`; initial Character and Cognition re-entry receive the same typed sections. `appendP8Correction` validates before the file adapter writes; loads only read and retain corruption as ERROR. Startup/reload use the same factory and persistent directory.                                               |
| Voice binding; voice person scope; acoustic persistence | Local intelligence calls the protected binding route. Both transcription/commit and `/v1/voice/message` use server-owned observations. Unresolved/mixed/conflicting observations cannot enable writes; explicit null scope cannot select another person's stored episodes. Acoustic list/identify are reads; enrollment/delete persist through SpeakerStore.                     |
| Memory vNext ABI; L0/L1/temporal/associative; Dream     | Runtime consumes `characterProjection` directly. Prompt assembly is read-only; completed-turn episode persistence honors write permission and filters excluded turns. Alternating people retain separate episodes and canonical scopes. Conversation restore still supplies recent narrative; no parsing of RelevantMemory into typed evidence remains.                          |
| Bounded Cognition; read-only capability                 | The sole non-test AppContext callback selects `executeProductionCognition`; simple Cognition has no product caller. The one-use Runtime path flows through the capability admission/normalized observation/continuation seam. File open is read-only, bounded and closed in finally; unavailable authorization exposes no tools.                                                 |
| Proactive                                               | Explicit streams and automatic wakes reach the same decision/continuation preparation. Configured identity now survives automatic admission and reload. Existing live listeners remain attached through AppContext. NO_OP does not persist episodes or assistant messages.                                                                                                       |
| STT/interruption; TTS/subtitles; fallback               | Media routes obtain the current registry per request; capture fences own committed STT observations. Companion speech queue calls TTS and publishes the selected sanitized subtitle segment only at playback start; SubtitlePage consumes the projection bus. Provider registry constructs the capability fallback wrappers; on-demand visual calls explicitly disable fallback. |
| Visual Grounding; Embodied Presentation                 | Character requests Runtime-owned capture with explicit-turn, revision, cancellation and one-cycle gates. The public image-analysis diagnostic returns provider output without claiming current-screen grounding or writing Memory. Embodied requests pass Runtime admission to the persistent AppContext bridge, device execution and correlated outcome ingress.                |
| Output Language; conversation/settings restart          | Settings reload constructs Character/Cognition with the next registry and parsed output language; pending restart settings retain their active values. Conversation/finalized-ingestion repositories survive Runtime replacement and are reopened on startup. STT language never writes output preference.                                                                       |
| Memory administration/maintenance                       | Recent/search/status routes stop at their read service/repository. Explicit mutations are controller routes. Maintenance dry-run skips stream recovery and writes; startup/periodic scheduling remains opt-in.                                                                                                                                                                   |
| Linux packaged start/restart                            | Packaged supervisor scripts call the real config/instance-lock/HTTP entrypoints; packaged Runtime runs migrations and `buildServer`. Supervisor passes `YUVI_RUNTIME_ENV_DIR` to the child. These script callers are not barrel-only exports.                                                                                                                                    |

The export scan was repeated independently of the inventory, then declarations without ordinary TypeScript callers were searched in entrypoint `index.ts`, `.mts` and `.mjs` scripts. `buildServer`, `loadServerConfig`, migration and supervisor constructors/locks/HTTP startup have real script callers; `pruneInactiveLogs` is called by the checkout supervisor script. Existing intentionally-library-only classifications remain applicable; neither barrel references nor tests were counted as production callers. The explicit image-analysis diagnostic is a live administration path, not a second conversational grounding path.

The fresh pass found no further unclosed gaps: `SEMANTIC_BYPASS = 0`, `PRODUCT_UNREACHABLE = 0`.

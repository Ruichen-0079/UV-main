# @companion/config

Typed runtime configuration helpers for AI Companion Runtime.

This package parses environment-shaped input into implementation-neutral configuration objects. It does not instantiate provider clients and must not import concrete provider classes.

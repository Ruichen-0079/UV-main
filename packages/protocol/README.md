# @companion/protocol

Shared event contracts for the runtime.

Responsibilities:

- Define event envelopes and payload schemas.
- Keep all runtime input and output represented as events.
- Provide runtime validation for transport boundaries.
- Avoid provider-specific payload leakage into core runtime events.
